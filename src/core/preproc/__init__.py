"""
As always should be provided, the data preprocesser utility

Otherwise, it seems that no one will actually use this framework without this

interface of feeder is also defined in 'core.py'

Ohter important utilization will be then defined

"""
